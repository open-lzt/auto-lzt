{
  "name": "bump-one-lot",
  "entry_node_id": "bump",
  "params": [
    {
      "key": "item_id",
      "label": "ID лота",
      "control": "number",
      "required": true,
      "default": 321,
      "description": "Какой лот поднимать в поиске"
    }
  ],
  "nodes": [
    {
      "id": "bump",
      "type": "market.bump",
      "inputs": { "item_id": { "literal": "{{vars.item_id}}" } }
    }
  ]
}
