# Runbooks

What cost time to find out, and would cost the same again.

Not a summary of changes — git holds those. These are traps, rules, and decisions.

- [Panel: three traps found in a browser](2026-07-20-panel-traps.en.md) — the kit's grid drops
  content into the sidebar slot; SSE is not open until a body byte ships; the market mock in
  `dev.py` answers every path identically.
